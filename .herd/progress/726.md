# Issue #726 — Address code review feedback (5 changes)

- [x] 1. Test gap — assert `HERD_PLAN_SCHEMA` in `TestPlan_EmptyInitialPromptUsesInteractive` (plan_test.go)
- [x] 2. Error UX — wrap `ReadPlanFile` error with finalize hint in `planInteractive` (plan.go)
- [x] 3. CHANGELOG — mention `HERD_PLAN_SCHEMA` alongside `HERD_PLAN_OUT`
- [x] 4. Nit — document "exists but unreadable = absent" policy on `authJSONPresent()` (auth.go)
- [x] 5. Nit — extract `allocatePlanArtifacts` helper; DRY both plan modes (plan.go)

## Verification
- [x] go build ./...
- [x] go test ./internal/agent/codex/... -count=1
- [x] go test ./... -count=1
- [x] go vet ./...
- [x] golangci-lint run
- [x] herd init --check (clean)
