# Issue #697 — Review fixes (cycle 3)

Gate the codex single-replica `replicas: 1` clamp on ChatGPT-subscription auth
so codex + plain API-key keeps the 3-replica default parallelism.

- [x] Export `CodexSubscriptionEnvSet()` in internal/config/validate.go
- [x] Add `CodexSubscription` field to `runnerTemplateData`
- [x] Gate template `replicas: 1` on `and (eq .Provider "codex") .CodexSubscription`
- [x] Thread `codexSubscription` bool through render/create chain in init.go
- [x] Production call sites pass `config.CodexSubscriptionEnvSet()`
- [x] Update existing tests for new param
- [x] Add `TestRenderDockerCompose_SingleReplica_CodexAPIKey` (codex API-key → replicas:3)
- [x] go build ./... — OK
- [x] go test ./... — OK
- [x] go vet ./... — OK
- [x] gofmt clean (golangci-lint unavailable: env Go-version mismatch)
