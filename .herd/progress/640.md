# Issue #640 — Documentation: restructure runners.md OpenCode auth section + configuration.md note

- [x] Restructure `### OpenCode provider` section in docs/runners.md into Option 1 / Option 2 layout
- [x] Option 1: API key (default, recommended) — preserves model->env table and adds recommended-default note
- [x] Option 2: ChatGPT subscription (opt-in) — install steps, device flow link, base64 encoding, `OPENCODE_AUTH_JSON` secret + .env, refresh-token caveat, volume persistence, community plugin links, TODO(verify) note
- [x] Add `OPENCODE_AUTH_JSON` row to Secrets Summary table in runners.md; note `OPENCODE_AUTH_FORCE_SEED` as env-only
- [x] Update Troubleshooting "Auth errors in worker" row to mention the subscription option
- [x] Update docs/configuration.md OpenCode example note to mention API key default + opt-in `OPENCODE_AUTH_JSON` subscription path
