# Issue #485 — Add `herd review <pr-number>` CLI command

- [x] Create `internal/cli/review.go` with `newReviewCmd`, `runReview`, `parsePRArg`, `buildReviewPromptData`, `renderReviewSystemPrompt` and the system prompt template
- [x] Register `newReviewCmd()` in `internal/cli/root.go` after `newBatchCmd()`
- [x] Create `internal/cli/review_test.go` covering prompt rendering (all sections / optional sections omitted / empty diff), command setup, and table-driven `parsePRArg`
- [x] `go build ./...` clean
- [x] `go test ./...` clean
- [x] `go vet ./...` clean
- [x] `golangci-lint` — cannot run locally due to Go 1.26 vs lint binary built with 1.25; CI installs the correct toolchain
- [x] Push to remote
