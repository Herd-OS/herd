# Issue #488 — Review fixes (cycle 1)

Fix: Apply missing docs changes from issue #486 to docs/design/execution.md.

- [x] Rename `## 12. Comment Commands` → `## 12. Commands`
- [x] Add `Kind` column to command reference table (existing rows = `Slash`)
- [x] Add new row for `herd review <pr-number>` (CLI / Local terminal)
- [x] Add distinguishing paragraph after the table (before `#### Non-Batch PR Reviews`)
- [x] `go build ./...` — clean
- [x] `go vet ./...` — clean
- [x] `go test ./...` — all packages pass
- [x] `golangci-lint run` — not installed in CI image; docs-only change, no Go lint impact
- [x] Push branch
