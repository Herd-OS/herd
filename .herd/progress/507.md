# Task 507 — Update documentation to reflect bugfix

## Status

No documentation changes required.

## Verification

- [x] Reviewed `README.md` — no references to the internal `--system-prompt` argv mechanism.
- [x] Reviewed `docs/installation.md` — no references to prompt-passing internals.
- [x] Reviewed `docs/getting-started.md` — only mentions "system prompt" at the user-facing level (e.g., `.herd/worker.md` is appended to the worker's system prompt). No mechanism details.
- [x] Reviewed `docs/configuration.md` — no references.
- [x] Reviewed `docs/runners.md` — no references.
- [x] Searched repo with `grep -r "--system-prompt" docs/ README.md` — no matches.
- [x] Reviewed `docs/design/architecture.md`, `docs/design/execution.md`, `docs/design/planner.md`, `docs/design/glossary.md`, `docs/design/github-integration.md`, `docs/design/vision.md`, `docs/design/roadmap.md` — none describe argv composition or the `claude` binary's flag set.
- [x] Confirmed no `CHANGELOG.md` or release-notes file exists at repo root or under `docs/`. Per task instructions, do not create one.
- [x] `go build ./...` passes.
- [x] `go test ./...` passes.
- [x] `go vet ./...` passes.
- [ ] `golangci-lint run` — local binary has a Go version mismatch (built against go1.25, repo targets go1.26.1); environmental, unrelated to this task. No code changes were made, so lint output is unaffected.

## Conclusion

The fix in PR #506 is purely internal: it changes how `internal/agent/claude/discuss.go` and `internal/agent/claude/plan.go` pass the system prompt to the `claude` binary (temp file via `--system-prompt-file` instead of inline `--system-prompt`). The herd CLI surface, configuration, and user-visible behavior are unchanged. No documentation references the internal argv composition, so no doc updates are warranted.
