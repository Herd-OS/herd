# Issue #629 — Fix CI failure on herd/batch/80

## Summary

CI's "Check workflow drift" step failed on batch PR #628 because
`.env.herd.example` was updated directly with OpenCode provider docs
without updating the embedded template at
`internal/cli/runner/.env.herd.example`.

## Checklist

- [x] Diagnose CI failure (workflow drift on `.env.herd.example`)
- [x] Update template `internal/cli/runner/.env.herd.example` to include OpenCode docs
- [x] Run `./herd init --check` (0 files drifted)
- [x] `go build ./...` clean
- [x] `go test ./... -count=1 -race` clean
- [x] `go vet ./...` clean
- [x] Commit and push
