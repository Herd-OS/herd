# Issue #675 — Drop subscription secrets from worker workflow

- [x] Remove OPENCODE_AUTH_JSON / OPENCODE_AUTH_FORCE_SEED env lines from `.github/workflows/herd-worker.yml`
- [x] Remove the same two lines (escaped) from `internal/cli/workflows/herd-worker.yml.tmpl`
- [x] Update `internal/cli/workflows_test.go` to assert the env vars are now absent (regression guard kept for OPENAI_API_KEY)
- [x] `grep -n 'OPENCODE_AUTH'` returns no matches in either target file
- [x] go build ./... — pass
- [x] go test ./... — pass
- [x] go vet ./... — pass
- [x] golangci-lint run — pass
- [x] `herd init --check` — 8 unchanged, materialized/template consistent
- [ ] Commit & push
