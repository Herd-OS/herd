# Issue #506 — Use --system-prompt-file for Discuss and Plan

- [x] Add unexported `writeSystemPromptFile` helper to `internal/agent/claude/claude.go`
- [x] Switch `Discuss` to write the system prompt to a temp file and pass `--system-prompt-file`
- [x] Switch `Plan` to do the same
- [x] Verify `review.go` was unaffected (it already passes the large prompt via stdin and only the small `reviewSystemPrompt` constant via argv)
- [x] `TestWriteSystemPromptFile` (table-driven: empty, small, >200KB)
- [x] `TestDiscuss_LargeSystemPrompt` and `TestDiscuss_TempFileCleanup` (skipped on Windows)
- [x] `TestPlan_LargeSystemPrompt` (skipped on Windows; success + failure subcases)
- [x] Existing tests still pass without modification
- [x] `go build ./...`, `go vet ./...`, `go test ./...` all pass
- [x] `gofmt -l` on changed files is clean
