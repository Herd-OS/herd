# Task 704 — Update docs + CHANGELOG, run full verification sweep

## Completed
- [x] docs/runners.md — collapsed Path B + Path C-shared + Path C-strict into a single
      'Path B — Personal subscription (Plus/Pro/Team/Edu)' section: single CODEX_AUTH_JSON
      flow, codex-auth named volume requirement, 'Running N parallel workers' subsection with
      `--scale worker=N` + refresh-race self-healing note, keepalive, recovery runbook,
      OpenAI security warnings. Path A (Enterprise CODEX_ACCESS_TOKEN) kept. Deleted all
      agent.codex_replicas / CODEX_AUTH_JSON_N / codex-replica / concurrency-cap text.
      Secret-routing rule keeps the CODEX_ACCESS_TOKEN-only Actions-secret rule.
- [x] docs/configuration.md — removed agent.codex_replicas table row, the codex_replicas: 1
      YAML line, and the entire 'Codex replicas' subsection (incl. #codex-replicas anchor
      link and the runners.md#subscription-auth-opt-in cross-ref in that subsection).
- [x] CHANGELOG.md — added a 'Removed' entry (Keep-a-Changelog style under [Unreleased]);
      reworded the Added subscription-auth entry to the single-CODEX_AUTH_JSON model.
- [x] Verification sweep:
      - go build ./...  -> exit 0
      - go vet ./...    -> exit 0
      - go test ./...   -> pass
      - gofmt -l        -> flags 32 pre-existing files (go1.26.1 toolchain comment-alignment
        drift); NONE are Go files this task touched (only docs/CHANGELOG changed). Not in scope.
      - golangci-lint   -> NOT installed in runner; relied on go vet + gofmt per task note.
      - herd init --check -> exit 0, no drift (8 unchanged).
      - straggler grep  -> only CHANGELOG.md matches (intentional historical reference).

## Remaining
- [ ] none — push.
