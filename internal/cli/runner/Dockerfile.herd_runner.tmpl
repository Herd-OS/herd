# HerdOS Runner — project customization
# This file is yours to customize — herd init will never overwrite it.
#
# Base image: {{.BaseImage}}
# This was auto-selected based on your project's manifest files.
#
# To add tools beyond the base flavor (e.g., a Postgres client, ImageMagick,
# a second language toolchain), add RUN apt-get install lines below.
#
# To change the base flavor, edit the FROM line above. Available flavors:
#   herd-runner-base    (minimal — OS + runner only)
#   herd-runner-node, herd-runner-ruby, herd-runner-python, herd-runner-go
#
# DO NOT COPY source code (checked out at job time) or bake in secrets.

FROM {{.BaseImage}}

# The base image runs as the 'runner' user. Switch to root to install
# packages, then switch back when done.
USER root

# Examples — uncomment what your project needs:
#
# RUN apt-get update && apt-get install -y postgresql-client && rm -rf /var/lib/apt/lists/*

USER runner
