# Issue #642: Fix CI failure on batch/83 cycle 1

CI was failing due to `herd init --check` drift detection.
The `.env.herd.example` and `docker-compose.herd.yml` templates were updated
in earlier batch-83 commits to include OpenCode subscription auth (#637, #638),
but the corresponding generated files at the repo root were never refreshed.

## Tasks

- [x] Identify drift between templates and generated files
- [x] Regenerate `.env.herd.example` to match `internal/cli/runner/.env.herd.example` (adds OpenCode auth lines)
- [x] Regenerate `docker-compose.herd.yml` to match `internal/cli/runner/docker-compose.herd.yml.tmpl` (adds OpenCode env passthrough and volume comment)
- [x] Verify `herd init --check` reports no drift
- [x] `go build ./...`
- [x] `go test ./...`
- [x] `go vet ./...`
- [x] `golangci-lint run`
- [x] Commit & push
