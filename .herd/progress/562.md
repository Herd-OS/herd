# Issue #562 — Add CascadeFailed label constant and registration

- [x] Add `CascadeFailed` constant to `internal/issues/labels.go` PR state labels block
- [x] Register `CascadeFailed` in `AllLabels()` with color `B60205` and description
- [x] Update `TestAllLabels` to assert presence of `CascadeFailed` with expected color/description
- [x] Add `IsStatusLabel(CascadeFailed)` assertion (false)
- [x] Add `HasLabel` assertions for `CascadeFailed`
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `golangci-lint run` passes
