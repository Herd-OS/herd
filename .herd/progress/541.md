# Issue #541 — Extend CheckCI in-progress guard

- [x] Broadened in-progress guard in `internal/integrator/ci.go` to cover review-fix, CI-fix, and conflict-resolution issues
- [x] Updated log line format to `Skipping CI fix: <kind> worker #<N> is still <status>`
- [x] Preserved `currentCycle` accumulation logic for the max-cycles cap
- [x] Added table-driven `TestCheckCI_SkipsWhenAnyFixWorkerInProgress` (covers all 3 categories × in-progress/ready)
- [x] Added `TestCheckCI_SkipsWhenReviewFixInProgress`
- [x] Added `TestCheckCI_SkipsWhenConflictResolutionInProgress`
- [x] Added `TestCheckCI_DispatchesWhenAllFixWorkersDone` (3 done fix issues, asserts new fix issue created with FixCycle=2)
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `golangci-lint run ./...` passes
