# Issue #688 — agent.codex_replicas config field + validation

- [x] Add `CodexReplicas int` field (yaml `codex_replicas`) to Agent struct (config.go)
- [x] Set `CodexReplicas = 1` default in Default() (defaults.go)
- [x] Add validation: must be >= 1; warn when >1 and provider != codex; max_concurrent <= codex_replicas in subscription mode (validate.go)
- [x] Add `codexSubscriptionEnvSet()` helper (validate.go)
- [x] Tests: TestValidate_CodexReplicasMinimum, TestValidate_MaxConcurrentBoundByReplicas (validate_test.go)
- [x] Tests: Default()==1 + YAML round-trip (config_test.go)
- [x] go build / go vet / gofmt -l / go test ./... / golangci-lint all pass
