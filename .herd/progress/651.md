# Issue #651 — Update runner env/workflow/compose comments for the Claude OAuth bridge

Comment-only changes; no new env var introduced. CLAUDE_CODE_OAUTH_TOKEN is reused.

## Completed
- [x] internal/cli/runner/.env.herd.example — Option 1 OAuth comment notes dual consumption by the OpenCode Anthropic subscription bridge (opencode-claude-auth); OpenCode section notes the subscription alternative to ANTHROPIC_API_KEY
- [x] .env.herd.example (root) — identical edits; verified byte-identical to the embedded copy
- [x] docker-compose.herd.yml — added comment above CLAUDE_CODE_OAUTH_TOKEN line describing dual purpose; value unchanged
- [x] internal/cli/runner/docker-compose.herd.yml.tmpl — same comment added to the embedded template so `herd init --check` stays green (root file is rendered from it)
- [x] internal/cli/workflows/herd-worker.yml.tmpl — left byte-identical; CLAUDE_CODE_OAUTH_TOKEN already surfaced (line 54)
- [x] go build ./... , go vet ./... , go test ./... all pass
- [x] herd init --check passes (0 files would be modified)

## Remaining
- [ ] none
