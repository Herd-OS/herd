# Task 602: Add `herd image build` / `herd image publish` commands

- [x] Create internal/cli/image.go with `image` parent + `build`/`publish` subcommands
- [x] `consumerRunnerImage(owner, repo, tag)` helper (lowercased GHCR ref)
- [x] Injectable `runCommand` package var for testability
- [x] build: errors when Dockerfile.herd_runner missing (directs to `herd init`)
- [x] publish: runs `docker push` for the same reference
- [x] `--tag` flag override, defaults to runnerImageTag(version)
- [x] Register newImageCmd() in internal/cli/root.go
- [x] Tests in internal/cli/image_test.go (reference formatting, build args, missing dockerfile, publish args, tag override)
- [x] go build ./..., go test ./..., go vet ./..., golangci-lint all pass
