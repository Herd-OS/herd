# Issue #674: Clean subscription auth from runner runtime config

- [x] Remove OpenCode-bridge comment lines from CLAUDE_CODE_OAUTH_TOKEN block in both .env.herd.example files
- [x] Remove anthropic-subscription comment block from OpenCode provider section in both .env files
- [x] Remove OpenCode ChatGPT subscription block (OPENCODE_AUTH_JSON / OPENCODE_AUTH_FORCE_SEED) from both .env files
- [x] Replace compose comment with '# Claude provider auth' in both compose files
- [x] Remove OPENCODE_AUTH_JSON / OPENCODE_AUTH_FORCE_SEED env lines from both compose files
- [x] Remove OpenCode auth.json persistence comment block from both compose files
- [x] Verify .env files byte-identical (diff clean)
- [x] Verify compose files differ only on REPO_URL line 20
- [x] go build / go test / go vet pass; herd init --check passes
