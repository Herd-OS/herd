# Issue #727 — Fix merge conflict + docs regression

## 1. Merge conflict with main (#723)
- [x] `git fetch origin` + `git merge origin/main`
- [x] Resolve `internal/agent/codex/auth.go` (modify/delete): keep file with only `authJSONPresent` + `resolveCodexHome`; drop provisioning machinery and `bytes`/`encoding/base64`/`sync` imports
- [x] `CHANGELOG.md` auto-merged — both PRs' Unreleased entries kept
- [x] `plan.go` auto-merged — dual-mode `Plan()` intact, no `ensureProvisioned()`
- [x] discuss/execute/review — clean (no conflicts)

## 2. Docs regression in docs/runners.md
- [x] `docker run` fallback no longer hardcodes `-u runner`; uses RUNNER_UID from .env
- [x] Added prose paragraph explaining UID-mismatch gotcha + 1001 default

## Verification
- [x] go build ./... clean
- [x] go vet ./... clean
- [x] go test ./... all green
- [x] herd init --check clean
- [ ] commit merge + push
