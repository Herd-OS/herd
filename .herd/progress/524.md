# Issue #524 — Review fixes (cycle 1)

- [x] Issue 1: change per-file display to `(would change)` for missing
      `.herdos.yml` and `Dockerfile.herd_runner` (other missing managed
      files already used the right wording).
- [x] Issue 2: `renderRunnerFiles` now propagates the merge error from
      `renderMergedCompose` and emits a stderr warning, so `--check`
      no longer reports spurious drift on `docker-compose.herd.yml`
      when an override fails to parse.
- [x] Issue 3: extract `computeManagedDrift` helper. Both
      `CheckHerdFilesUpToDate` and `runInitCheck` now share a single
      pass, removing the duplicate `loadConfigForCheck` /
      `renderManagedFiles` calls in the check path.
- [x] Added tests:
      `TestRunInitCheck_DisplayUsesWouldChangeForMissingFiles`,
      `TestRunInitCheck_WarnsOnUnparseableOverride`,
      `TestComputeManagedDrift_ReturnsRenderedFilesAndDrift`.
- [x] `go build`, `go test ./...`, `go vet`, `golangci-lint` all green.
