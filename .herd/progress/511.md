# Issue #511 — Pre-dispatch active-run check in dispatchReadyIssues

- [x] Replace single `ListRuns(in_progress)` with two calls (`in_progress` + `queued`) and combine results
- [x] Build `activeByIssue` map keyed on `run.Inputs["issue_number"]`
- [x] Insert per-issue duplicate check BEFORE the unblock-relabel logic; warn and `continue` without label changes
- [x] Update `mockWorkflowService` to support per-status `listResultByStatus` and capture filter history
- [x] Add `TestDispatchReadyIssues_SkipsAlreadyInProgress` (table-driven) covering: in_progress skip, queued skip, no-active dispatch, mixed batch, capacity-limit
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `golangci-lint run` passes (built v1.64.8 from source against go1.26.1 to match CI behavior)
