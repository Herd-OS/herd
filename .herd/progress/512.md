# Task 512 — Docs review for duplicate-dispatch fix

## Status

Documentation updated.

## Files reviewed

- [x] `docs/configuration.md` — no references to retry transport, dispatch idempotency, or active-run handling. No changes needed.
- [x] `docs/getting-started.md` — describes user-visible dispatch flow; does not document the HTTP retry transport or the integrator's pre-dispatch active-run check at this level of detail. No changes needed.
- [x] `docs/installation.md` — no references. No changes needed.
- [x] `docs/runners.md` — discusses runner setup and `HERD_GITHUB_TOKEN` for `workflow_dispatch`, but not retry behavior or dedup. No changes needed.
- [x] `docs/design/README.md` — index file. No changes needed.
- [x] `docs/design/architecture.md` — high-level component map; does not describe retry transport or dispatch dedup. No changes needed.
- [x] `docs/design/execution.md` — describes worker lifecycle, retry-resume from progress files, monitor re-dispatch, and stale-ready dispatch. The "retry" terminology here refers to worker-level re-dispatch, not the HTTP transport, so no changes needed.
- [x] `docs/design/github-integration.md` — **updated**. Two sections touched:
  - "Automatic Retry on Transient Errors" (§5) — added the workflow_dispatch opt-out from the retry transport with the rationale (non-idempotent; GitHub may queue then 5xx).
  - "Concurrency and Race Conditions / Double-dispatch prevention" (§3) — added the `dispatchReadyIssues` belt-and-suspenders active-run check (lists `in_progress` and `queued` runs, skips by issue number, leaves labels untouched).
- [x] `docs/design/glossary.md` — defines status labels but does not describe dispatch internals. No changes needed.
- [x] `docs/design/planner.md` — planner-side dispatch description; does not describe integrator-side dedup. No changes needed.
- [x] `docs/design/roadmap.md` — no relevant references. No changes needed.
- [x] `docs/design/vision.md` — discusses NDI principle generally; no concrete retry/dispatch internals. No changes needed.
- [x] `docs/examples/*.yml` and `docs/examples/README.md` — config samples only, no narrative on retry/dispatch internals. No changes needed.

## Verification

- [x] Reviewed every file under `docs/`.
- [x] `grep -in "retry|workflow_dispatch|idempoten|in_progress|queued"` (case-insensitive) across `docs/` confirmed the two passages above are the only places that describe HTTP retry behavior or worker-dispatch dedup at the implementation level.
- [x] No code files outside `docs/` modified.
- [x] No new top-level READMEs created.
- [x] `go build ./...` passes.
- [x] `go test ./...` passes.
- [x] `go vet ./...` passes.
- [x] `golangci-lint run` passes (no Go code touched).

## Conclusion

Tasks 0 and 1 (#510, #511) introduced two changes that are visible in the design documentation: the retry transport now skips workflow_dispatch POSTs, and the integrator's `dispatchReadyIssues` does a pre-dispatch active-run check. Both are surfaced in `docs/design/github-integration.md` in the sections that already describe HTTP retry behavior and double-dispatch prevention. No other docs needed changes; user-facing docs do not describe these internals.
