- [x] Merged latest origin/main; branch was already up to date with no conflicts.
- [x] Fixed command acknowledgement redelivery to avoid duplicate ack comments after ack metadata persistence failures.
- [x] Fixed workflow dispatch duplicate handling for completed mutation repair and post-accept persistence failure classification.
- [x] Fixed worker patch apply retry metadata recovery so final JobResult includes the original commit SHA without reapplying.
- [x] Normalized task issue labels for idempotency fingerprints.
- [x] Fixed workflow event post-processor-success repair so redelivery does not rerun the processor after status marker failures.
- [x] Verified focused control-plane packages and required reviewdiff/git/github/integrator/cli suite.
- [x] Verified `go build ./...`, `go test ./...`, and `go vet ./...`.
- [ ] `golangci-lint run` could not be completed because the binary is not installed and `go run` builds the linter with Go 1.25 while the module targets Go 1.26.1.
