- [x] Added App parser support for `resolve-conflicts` and `dispatch`.
- [x] Removed unsupported App parser/docs surface for `retry` and `integrate`.
- [x] Routed `resolve-conflicts` through hosted App issue creation, dedupe, mergeability checks, and durable conflict-resolution dispatch.
- [x] Routed `dispatch` through hosted App issue validation, label transition, and durable worker dispatch.
- [x] Added focused parser and handler tests.
- [x] Added production dispatcher tests for hosted resolve-conflicts and dispatch behavior.
- [x] Run full validation: `go build ./...`, `go test ./...`, `go vet ./...`, `golangci-lint run`.
