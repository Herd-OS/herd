# Issue #686 — Unpin @openai/codex in Dockerfile

- [x] Drop version pin + TODO(verify) comment from `images/base/Dockerfile`
- [x] Update `agentNpmPackages` in `internal/cli/image_test.go` to bare `@openai/codex`
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `TestDockerfile_BakesAgentNpmPackages` passes
- [x] `gofmt -l` clean on changed file
- [x] `herd init --check` passes (no drift)
- [x] Commit and push

Note: golangci-lint not installed in this environment; build/vet/test all clean.
