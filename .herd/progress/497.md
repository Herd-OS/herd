## Issue #497: Review and update docs for new herd init PR messaging

### Completed
- [x] Searched all docs/ files and README.md for `herd init`, `Update HerdOS`, and `.herd/state` references.
- [x] Verified the implementation in `internal/cli/init.go` writes `.herd/state/version` and selects between `Install HerdOS <v>`, `Update HerdOS to <v>`, and `Sync HerdOS files` PR titles.
- [x] Updated `docs/getting-started.md` with a one-sentence note that `.herd/state/version` (gitignored) is recorded and used to decide between install/update/sync on subsequent runs.
- [x] Reviewed `docs/installation.md`, `docs/configuration.md`, `docs/runners.md`, and `README.md` — none currently quote a PR title, so per the task's instruction to not invent a section just to mention this, no changes were made there.
- [x] Confirmed no stale `Update HerdOS to` references contradict the new behavior (`grep` shows only the new doc reference).
- [x] Ran `go build ./...`, `go vet ./...`, `go test ./...`, and `golangci-lint run` — all clean.
