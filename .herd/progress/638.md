# Issue #638 — Install auth plugin in entrypoint + wire runner .env and docker-compose

- [x] Resolve pinned version of `opencode-openai-codex-auth` via `npm view` → `4.4.0`
- [x] `images/base/entrypoint.herd.sh`: insert plugin install line immediately after opencode-ai install, with TODO(verify) comment
- [x] `internal/cli/runner/.env.herd.example`: document `OPENCODE_AUTH_JSON` and `OPENCODE_AUTH_FORCE_SEED` as commented opt-in vars under the existing OpenCode block
- [x] `internal/cli/runner/docker-compose.herd.yml.tmpl`: pass `OPENCODE_AUTH_JSON` and `OPENCODE_AUTH_FORCE_SEED` through worker env; add commented volume mount for OpenCode data dir
- [x] `bash -n images/base/entrypoint.herd.sh` passes
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `golangci-lint run ./...` passes
