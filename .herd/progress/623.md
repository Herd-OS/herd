# Issue #623 — Documentation for multi-provider / OpenCode support

- [x] Update `docs/configuration.md` — document `agent.provider` values `claude` and `opencode`, add an Agent Providers reference section explaining `binary`, `model` (provider/model form for opencode), and that `max_turns` is ignored by opencode. Include an OpenCode example YAML block.
- [x] Update `docs/runners.md` — note that the base image installs both Claude Code and OpenCode, document per-provider auth (Claude vs OpenCode model-prefix → env var), update secrets summary, cloud-runner section, and troubleshooting rows.
- [x] Update `docs/design/roadmap.md` — adjust v1 limitations and v2 Multi-Agent sections to mark OpenCode as implemented while keeping Codex/Cursor/Gemini planned.
- [x] Update `README.md` — add OpenCode to the enumerated agent list in the "How it works" paragraph.
- [x] Verify YAML example parses correctly.
- [x] Commit and push.
