# Issue #544 — Review fixes (cycle 1)

## Reviewer concern

Edge case (b): no test verifies that an issue body which fails to parse
(`issues.ParseBody` returns error) is silently skipped without panicking.

## Plan

- [x] Locate the parse-error path in `internal/integrator/ci.go` (line 113-116).
- [x] Confirm what kinds of YAML front matter trigger `ParseBody` to return an error.
- [x] Add `TestCheckCI_SilentlySkipsUnparseableIssueBody` to `internal/integrator/ci_test.go`:
  - Uses a deliberately malformed YAML front matter (`this is not: : valid yaml [`).
  - Sanity-checks the body actually fails `issues.ParseBody` (so the test exercises
    the parseErr branch).
  - Wraps the `CheckCI` call in `require.NotPanics` to assert no panic.
  - Asserts the unparseable issue is *not* treated as an in-progress fix worker:
    even though the issue carries `StatusInProgress`, CheckCI proceeds to create and
    dispatch a new CI fix issue.
- [x] `go build ./...` — passes
- [x] `go test ./...` — passes (full suite)
- [x] `go vet ./...` — passes
- [x] `golangci-lint run` — local env mismatch (binary built with go 1.25, project
      targets go 1.26.1). CI uses `install-mode: goinstall` so it will rebuild
      against go.mod and run cleanly there.
- [x] Commit and push.
