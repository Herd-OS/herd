# Issue #500 — Document release self-init step

- [x] Survey existing docs (`docs/getting-started.md`, `docs/installation.md`,
      `docs/configuration.md`, `docs/runners.md`, `docs/design/`, `docs/examples/`).
- [x] Confirm README has no maintainer/contributing section — leave README untouched.
- [x] Decide location: append `Release process (maintainers)` section to
      `docs/installation.md` (release content is closely tied to where
      binaries come from; size does not warrant a separate file).
- [x] Document `release` job (binaries, Homebrew, GitHub Release, artifact upload).
- [x] Document `self-init` job (artifact download, `herd init --skip-labels`,
      PR title `Update HerdOS to <tag>`, branch `herd/init-<tag>`).
- [x] Document the no-op-skip behavior and the exact log line.
- [x] Document required permissions and the
      `${{ secrets.HERD_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}` token expression.
- [x] Mention test coverage (`TestRunInitSkipLabelsEndToEnd`,
      `TestRunInitSkipLabelsIdempotent` in `internal/cli/init_test.go`).
- [x] Audit other docs for `softprops/action-gh-release` / release-workflow
      references; runners.md's allowlist note is unrelated to herd's own release
      workflow and is out of scope for this task.
- [x] Verify no broken internal links (`grep -r '\](docs/' .`).
- [x] Run `go build ./...`, `go test ./...`, `go vet ./...`, `golangci-lint run`.
- [x] Push branch.
