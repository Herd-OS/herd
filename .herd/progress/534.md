# Issue #534: Fix issue_number key + render title in active workers panel

- [x] Fix `r.Inputs["issue"]` -> `r.Inputs["issue_number"]` in `internal/dashboard/fetch.go`
- [x] Update `workersPanel` in `internal/dashboard/view.go` to render `#N <title> (elapsed)` with truncation
- [x] Add `formatElapsed` helper in `internal/dashboard/view.go`
- [x] Update existing `TestFetch_PopulatesWorkersFromRuns` to use `issue_number` key
- [x] Add `getCalls` counter to `fakeIssueService` in `internal/dashboard/fetch_test.go`
- [x] Add `TestFetch_PopulatesIssueNumberFromRunInputs` test
- [x] Add `TestFetch_HandlesMissingIssueNumber` test (with `getCalls == 0` assertion)
- [x] Create `internal/dashboard/view_test.go` with all four `TestRenderWorkers_*` tests
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `go vet ./...` passes
- [x] `golangci-lint run ./...` passes
