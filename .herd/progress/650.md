# Task 650 — Install opencode-claude-auth plugin in runner entrypoint

- [x] Add `npm install -g --no-audit --no-fund opencode-claude-auth@1.5.4` after opencode-openai-codex-auth install
- [x] Mirror TODO(verify) comment style for the new plugin line
- [x] Verify version via `npm view opencode-claude-auth version` (1.5.4)
- [x] Confirm bash syntax (`bash -n`) passes
- [x] New line sits before the `echo "Installed opencode ..."` line
