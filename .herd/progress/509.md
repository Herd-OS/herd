# Issue #509 - Fix CI lint failure on batch/57

## Failure

`golangci-lint` errcheck on the batch branch:

- `internal/agent/claude/discuss.go:25:17` — `defer os.Remove(promptFile)` (return value unchecked)
- `internal/agent/claude/plan.go:200:17` — `defer os.Remove(promptFile)` (return value unchecked)

## Plan

- [x] Reproduce the lint failure (read CI logs from run 25446202089)
- [x] Wrap each `defer os.Remove(...)` in a closure that discards the return value (matches existing pattern in `claude.go`)
- [x] `go build ./...`
- [x] `go test ./...`
- [x] `go vet ./...`
- [x] `golangci-lint run ./...`
- [ ] Commit & push to worker branch
