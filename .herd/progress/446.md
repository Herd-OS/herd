- [x] Move renderWorkerPrompt and execOpts inside else branch so skip path avoids spurious errors
- [x] Handle retry path that also needs execOpts by building it locally before retry
- [x] Verify all tests pass
